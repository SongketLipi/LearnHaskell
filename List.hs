-- Define a list buy putting it in a square bracket
listA = [69,420,121]
listB = [121,1,2]

-- Concat multiple list by adding `++` between them
listC = listA ++ listB

stringA = "alpha"
stringB = "beta"

charListA = ['a','b','c','d']
charListB = ['c','d','e','f']

-- prepend an element by adding it to list by putting `:` before the list
charListC = 'a':charListA
prepending = 1:2:3:[]